.env
.env.local
node_modules/
.DS_Store
*.tar.gz
test-results.json
server/test-70b-results.json
server/quality-test-results.json

# Capacitor build output (regenerated by `npm run build:www`)
www/

# Local model files for on-device testing (see models/README.md). Never
# commit a .task/.litertlm/.bin/.tflite/.gguf model — they run 0.5-3 GB and
# the app already has a real on-phone import flow (⚙ → Local Gemma / Local
# AI → Import model) that doesn't need one in git at all.
models/*.task
models/*.litertlm
models/*.bin
models/*.tflite
models/*.gguf
